#include "main.h"
#include <string.h>

/**
 * _strlen - Return the length of a string.
 *
 * @s: the string that its when will be checked
 *
 * Return: Always success
 *
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}

