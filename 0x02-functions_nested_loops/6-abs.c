#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value to an integer
 *
 * @n: The number that will be computed
 *
 * Return: Always 0.
 */

int _abs(int n)
{
	return (abs(n));
}
