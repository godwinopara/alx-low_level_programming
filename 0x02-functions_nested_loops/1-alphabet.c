#include "main.h"
/**
 * main - Entry point
 *
 * Description: prints alphabet in lowercase
 * followed by newline
 *
 * Return: Always success.
 */

int main(void)
{
	char low = a;

	while (low <= z)
	{
		_putchar(low);
		low++;
	}
	_putchar("\n");
	return (0);
}
